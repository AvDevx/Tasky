{
  "name": "tasky",
  "displayName": "tasky",
  "description": "A todo list for distributed projects",
  "version": "0.0.2",
  "engines": {
    "vscode": "^1.92.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "tasky.addNote",
        "title": "Add Note"
      },
      {
        "command": "tasky.openNote",
        "title": "Open Note"
      }
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AvDevx/Tasky.git"
},
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "@types/vscode": "^1.92.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.9.1"
  },
  "dependencies": {
    "uuid": "^10.0.0"
  }
}
